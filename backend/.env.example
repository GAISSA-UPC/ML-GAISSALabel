# Django Environment Configuration
# CRITICAL: This file contains sensitive data - NEVER commit to Git!
# Copy this to .env and fill in your actual values

# Security Settings
DJANGO_SECRET_KEY=your-secret-key-here-generate-a-new-one
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,gaissalabel.essi.upc.edu

# Database (REQUIRED - no defaults)
DATABASE_NAME=bdgaissa_local
DATABASE_USER=your-database-username
DATABASE_PASSWORD=your-database-password
DATABASE_HOST=localhost
DATABASE_PORT=5432

# Frontend URL
URL_FRONTEND=http://localhost:5173

# Hugging Face API Token (Optional - improves API rate limits)
# Get your token from: https://huggingface.co/settings/tokens
HUGGINGFACE_API_TOKEN=your-huggingface-token-here

# Generate a new SECRET_KEY with:
# python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
